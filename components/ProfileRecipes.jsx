import React from 'react'

const ProfileRecipes = ({q}) => {
    console.log(q);
    
  return (
    <div>
      pr
    </div>
  )
}

export default ProfileRecipes

import React from 'react'

const ProfileRecipes = ({q}) => {
  return (
    <div>
      
    </div>
  )
}

export default ProfileRecipes
